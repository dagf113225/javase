properties文件，微服务的核心配置文件.

Properties类

xml文件，作为之前的java首选框架配置文件，逐渐被注解取代。

xml文件的应用和解析
1.什么是xml文件  ，后缀名是xml
2.文件头不能有空行
3.文件头必须有xml声明
4.有开始标记必须有结束标记
5.必须有一个根元素
6.必须正确的嵌套，必须区分大小写。

作用:1.xml作为数据交换
     2.xml作为配置文件
     
     xml解析5种:
     
     1.DOM 
     2.SAX
     3.JDOM
     4.DOM4J   ****** apache
     5.PULL

json交互数据的文件或数据格式。